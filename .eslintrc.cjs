module.exports = {
  env: {
    browser: true,
    commonjs: true,
    es2021: true
  },
  extends: 'standard',
  overrides: [],
  parser: '@babel/eslint-parser',
  parserOptions: {
    ecmaVersion: 'latest',
    requireConfigFile: false,
    babelOptions: {
      plugins: ['@babel/plugin-syntax-import-assertions']
    }
  },
  plugins: ['spellcheck'],
  rules: {
    semi: [2, 'always'],
    'no-console': process.env.NODE_ENV === 'production' ? 'error' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'error' : 'off',
    'space-before-function-paren': [0, 'never'],
    'n/no-callback-literal': 'off',
    indent: ['off', 0],
    'no-case-declarations': ['off', 0],
    'array-callback-return': ['off', 0],
    camelcase: [0, { allow: ['camel_case', 'snake_case'] }],
    'spellcheck/spell-checker': [
      1,
      {
        comments: false,
        strings: false,
        templates: true,
        skipWords: [
          'upsert',
          'reqParam',
          'req',
          '$gte',
          '$lte',
          'devicetype',
          'devicetoken',
          'appversion',
          'cond',
          '$lt',
          'Joi',
          'cors',
          'urlencoded',
          'ip',
          'fcm_options',
          'Verifier',
          'verifier',
          'cron',
          'pathname',
          'winston',
          'awsAccessKeyId',
          'awsSecretKey',
          'awsRegion',
          'encryptDecryptPayload',
          'createDecipheriv',
          'decryptedData',
          'createCipheriv',
          'facebook',
          'instagram',
          'snapchat',
          'tiktok',
          'youtube',
          'dict',
          'aff',
          'hunspellchecker',
          'hunspell',
          'utils',
          'md',
          'sha',
          'io',
          'redis',
          'ksp',
          'str',
          'nin',
          'ack',
          'ttl',
          'mumbai',
          'openapi',
          'apis',
          'paris',
          'acc',
          'Behaviour',
          'xp',
          'printf',
          'msg',
          'egypt',
          'resave',
          'localhost'
        ],
        skipIfMatch: ['http://[^s]*', '^[-\\w]+/[-\\w\\.]+$'],
        skipWordIfMatch: ['^unix.*$', '^foobar.*$', '^powerup.*$', '^Powerup.*$']
      }
    ]
  }
};
